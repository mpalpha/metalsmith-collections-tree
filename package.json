{
  "name": "metalsmith-collections-tree",
  "version": "1.0.1",
  "description": "create nested metadata from the metalsmith-collections global metadata",
  "main": "lib/index.js",
  "repository": "git://github.com/mpalpha/metalsmith-collections-tree.git",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.4",
    "metalsmith": "^2.3.0",
    "metalsmith-collections": "^0.9.0"
  },
  "devDependencies": {
    "metalsmith": "2.x",
    "mocha": "^3.1.2"
  },
  "scripts": {
    "pretest": "npm install --silent --progress=false",
    "test": "mocha --reporter spec"
  }
}
